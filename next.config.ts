import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['image.flaticon.com', 'images.unsplash.com', 'image.vecteezy.com', 'www.clipartmax.com'],
  },
};

export default nextConfig;
